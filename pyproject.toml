[project]
name = "skills"
version = "0.1.0"
description = "Portable Agent Skills with manifest-driven external skill vendoring."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
